services:
  backend:
    build: .
    ports:
      - "8080:8080"
    environment:
      - STEAM_API_KEY=${STEAM_API_KEY}
      - PORT=8080
    volumes:
      - ./data:/app/data
    restart: unless-stopped
